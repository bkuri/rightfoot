doctype html

html(lang=lang)
  include ./partials/head.jade

  body
    include assets/foot.svg

    ol
      li Local variables can be found inside the brunch config file:
        pre= urls.brunch

      li The main HTML (what you're reading right now) can be changed by editing:
        pre= urls.jade

      li The main stylesheet is located at:
        pre= urls.styl

      li The main coffeescript file is located at:
        pre= urls.coffee

      li You can install packages with bower like so (inside the base directory):
        pre bower install --save
          i  packagename
