doctype html

include ./partials/properties.jade

html
  head
    title= app.name

    meta(charset="utf-8")

    meta(
      name="viewport"
      content="width=device-width, minimum-scale=1.0, initial-scale=1.0, user-scalable=yes"
    )

    script(type="text/javascript", src="js/vendor.js")
    script(type="text/javascript", src="js/app.js")

    link(rel="stylesheet", href="css/app.min.css")

  body
    h1= app.name
    h2 Quick notes

    ol
      li The main HTML (what you're reading right now) can be changed by editing:
        pre ./index.jade

      li The main stylesheet is located at:
        pre ./styles/app.styl

      li The main JS is located at:
        pre ./scripts/app.coffee

      li You can install packages with bower like so (inside the base directory):
        pre bower install --save
          i  packagename
